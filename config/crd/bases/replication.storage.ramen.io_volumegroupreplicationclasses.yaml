---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: volumegroupreplicationclasses.replication.storage.ramen.io
spec:
  group: replication.storage.ramen.io
  names:
    kind: VolumeGroupReplicationClass
    listKind: VolumeGroupReplicationClassList
    plural: volumegroupreplicationclasses
    shortNames:
    - vgrc
    singular: volumegroupreplicationclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provisioner
      name: Provisioner
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VolumeGroupReplicationClass is the Schema for the volumegroupreplicationclasses
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeGroupReplicationClassSpec defines the desired state
            properties:
              parameters:
                additionalProperties:
                  type: string
                description: Driver specific parameters
                type: object
              provisioner:
                description: |-
                  Storage provisioner that this replication class supports
                  Example: rook-ceph.cephfs.csi.ceph.com
                type: string
            type: object
          status:
            description: VolumeGroupReplicationClassStatus defines the observed state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
